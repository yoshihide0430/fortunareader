name: fortuna_reader
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  sensors_plus: ^5.0.0
  share_plus: ^10.0.2
  flutter:
    sdk: flutter
  just_audio: ^0.9.36
  audio_session: ^0.1.19
  firebase_core: ^4.2.1
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    # 🔮 星座占いデータ（12セット×180件）
    # Homeの「星座占い」機能で使うJSON
    - assets/zodiac/zodiac_sets.json

    # 🧧 おみくじ結果データ
    # OmikujiBox → 結果画面で読み込む予定のJSON
    # まだ中身未完成なら、ファイルだけ空で置いておく or 一旦この行をコメントアウトでもOK
    - assets/omikuji/fortunes.json

    # 🎵 BGM（アプリ全体で流すループBGM）
    - assets/audio/n59.mp3

    # 📦 おみくじ箱＆棒の画像（おみくじ画面用）
    - assets/images/omikuji_box.png     # 箱本体
    - assets/images/omikuji_stick.png   # 引き抜く棒の画像

    # 🔊 効果音（おみくじ演出用）
    - assets/audio/shake.mp3  # 振るとき
    - assets/audio/stick.mp3  # 棒が出るとき
    - assets/audio/paper.mp3  # 紙が開くとき

    # 🏠 ホーム画面アイコン（4つのカード）
    - assets/images/palm.png      # 手相
    - assets/images/zodiac.png    # 星座占い
    - assets/images/daily.png     # 今日の運勢
    - assets/images/omikuji.png   # おみくじ

    # 🎴 おみくじ結果背景（和紙っぽい背景）
    - assets/images/omikuji_bg.png


  # フォントを追加する場合はここに追記
  # fonts:
  #   - family: NotoSansJP
  #     fonts:
  #       - asset: assets/fonts/NotoSansJP-Regular.ttf
  #       - asset: assets/fonts/NotoSansJP-Bold.ttf
