name: fortuna_reader
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  sensors_plus: ^5.0.0
  share_plus: ^10.0.2
  flutter:
    sdk: flutter
  just_audio: ^0.9.36
  audio_session: ^0.1.19
  firebase_core: ^4.2.1
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/omikuji/fortunes.json
    # 🎵 BGMファイル
    - assets/audio/n59.mp3
    - assets/audio/sfx_shake.mp3      # 箱を振る
    - assets/audio/sfx_stick.mp3      # 棒が当たる

    # 🖼️ 画像類
    - assets/images/palm.png
    - assets/images/zodiac.png
    - assets/images/daily.png
    - assets/images/omikuji.png
    - assets/images/omikuji_bg.png

  # フォントを追加する場合はここに追記
  # fonts:
  #   - family: NotoSansJP
  #     fonts:
  #       - asset: assets/fonts/NotoSansJP-Regular.ttf
  #       - asset: assets/fonts/NotoSansJP-Bold.ttf
